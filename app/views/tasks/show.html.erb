<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<ul>
  <li><%= link_to "Edit the task", edit_task_path(@task) %></li>
  <li><%= link_to "Delete the task", delete_path(@task),
  method: :delete,
  data: {confirm: "Are you sure?"} %>
  </li>
</ul>

